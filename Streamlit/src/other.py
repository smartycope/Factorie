import streamlit as st
from streamlit import session_state as ss


"This is just some other stuff, like the app description, which obviously won't be in the app itself, but I would still like your feedback on"

with st.expander("App Description"):
    """
    <title of app> is an app to help you make decisions!
    """